import AddFriendButton from '@/components/AddFriendForm';
import { FC } from 'react';

const Page = ({}) => {
  return (
    <main className="pt-8">
      <AddFriendButton />
    </main>
  );
};

export default Page;
